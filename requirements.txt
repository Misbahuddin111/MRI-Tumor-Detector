cat > requirements.txt << 'EOF'
streamlit
tensorflow
numpy
pillow
plotly
pandas
opencv-python-headless
EOF

git add requirements.txt
git commit -m "Add TensorFlow for real predictions"
git push origin main